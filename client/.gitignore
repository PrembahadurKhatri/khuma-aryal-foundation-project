node_modules
dist
dist-ssr
*.local
.env
.DS_Store

# Regenerated by scripts/generate-sitemap.mjs as an npm prebuild step (see
# package.json) -- always fresh in dist/ after a build, so committing a
# snapshot here would just go stale between deploys.
public/sitemap.xml
